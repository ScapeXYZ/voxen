"use client";
import { useState } from "react";
import Link from "next/link";
const nodes = [["Create proposal","Set a clear question, choices, timing, and who can vote.","/create"],["Check eligibility","The contract checks GEN or a credential when a vote is cast.","/explore"],["AI governance review","Guarded Community proposals are reviewed against their rules.","/communities"],["Community votes","Each eligible wallet has one vote.","/explore"],["Final result","After close, the creator finalizes a winner or a tie.","/live-proof"]] as const;
export function GovernanceGraph(){const [active,setActive]=useState(0); return <section className="governance-graph" aria-label="Voxen governance flow"><ol>{nodes.map(([name,detail,href],i)=><li key={name}><button onClick={()=>setActive(i)} aria-pressed={active===i}><span>0{i+1}</span><strong>{name}</strong></button>{i<nodes.length-1&&<i aria-hidden="true">↓</i>}</li>)}</ol><article aria-live="polite"><h2>{nodes[active][0]}</h2><p>{nodes[active][1]}</p><Link className="text-link" href={nodes[active][2]}>Learn more →</Link></article></section>}
